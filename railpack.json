{
  "$schema": "https://schema.railpack.com",
  "provider": "python",
  "deploy": {
    "startCommand": "cd backend && python -u -c \"import os,time; print('NEXORA_RUNTIME_START', flush=True); print('PORT=', os.environ.get('PORT'), flush=True); print('DJANGO_SETTINGS_MODULE=', os.environ.get('DJANGO_SETTINGS_MODULE'), flush=True); print('BOOT_OK', flush=True); time.sleep(600)\""
  }
}
