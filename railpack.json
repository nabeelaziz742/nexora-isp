{
  "$schema": "https://schema.railpack.com",
  "provider": "python",
  "deploy": {
    "startCommand": "cd backend && python -u -c \"import os; print('NEXORA_SIMPLE_SERVER_START', flush=True); print('PORT=', os.environ.get('PORT'), flush=True)\" && python -m http.server ${PORT:-8080} --bind 0.0.0.0"
  }
}
